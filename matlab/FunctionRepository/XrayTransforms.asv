% Script stores/ generates some function handles to use

% variables:
r = 0.5;
R = 1;



% Case: Euclidean Metric, Circle Domain /w radius R, integrand inFunc0
inFunc0 = @(x,y) x.^2+y.^2 < r^2;
xray0 = @(beta,alpha) 2.*sqrt( r.*r - R.*R.*sin(...
                               abs(max( min(alpha,asin(r/R)) ,0))...
                                             ).^2 );